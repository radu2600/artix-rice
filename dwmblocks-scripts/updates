#!/bin/bash

# Run checkupdates and capture its output
cupd=$(checkupdates 2>/dev/null | wc -l)

# Find and kill any faked processes owned by current user
pgrep -u $USER faked | xargs -r kill -9 2>/dev/null

echo ": $cupd"
